{#

This file is part of the SensioLabs Admin Bundle package.

(c) SensioLabs

For the full copyright and license information, please view the LICENSE
file that was distributed with this source code.

#}

{% if admin.hasAccess('edit', object) and admin.hasRoute('edit') %}
    {% set button_content = sensiolabs_config.getOption('list_action_button_content') %}
    <a
        href="{{ admin.generateObjectUrl('edit', object, actions.link_parameters|default([])) }}"
        class="inline-flex items-center justify-center gap-1.5 p-2 rounded-lg text-primary-600 hover:text-primary-700 hover:bg-primary-50 transition-colors edit_link"
        title="{{ 'action_edit'|trans({}, 'SensioLabsAdminBundle') }}"
    >
        {% if button_content == 'icon' or button_content == 'all' %}
            {{ ux_icon('lucide:pencil', {class: 'w-4 h-4'}) }}
        {% endif %}
        {% if button_content == 'text' or button_content == 'all' %}
            <span class="text-sm font-medium">{{ 'action_edit'|trans({}, 'SensioLabsAdminBundle') }}</span>
        {% else %}
            <span class="sr-only">
                {{ 'action_edit'|trans({}, 'SensioLabsAdminBundle') }}
            </span>
        {% endif %}
    </a>
{% endif %}

{#

This file is part of the SensioLabs Admin Bundle package.

(c) SensioLabs

For the full copyright and license information, please view the LICENSE
file that was distributed with this source code.

#}

{% if object is not null
    and admin.id(object) is not null
    and admin.hasAccess('history', object)
    and admin.hasRoute('history')
%}
    <a class="admin-btn-ghost" href="{{ admin.generateObjectUrl('history', object) }}">
        {{ ux_icon('lucide:clock', {class: 'w-4 h-4'}) }}
        {{ 'link_action_history'|trans({}, 'SensioLabsAdminBundle') }}
    </a>
{% endif %}
